mixin text(tag,string)
  //- &attributes adds passed attributes to a teg
  -var elClass = attributes.class;
  

  //- 
    text class is default text style setted in main.scss for html and body tags, so I removed it 
    #{tag}(class=`text ${elClass}`)=string
  if elClass
    #{tag}(class=`${elClass}`)=string
  else
    #{tag}(class='')=string
